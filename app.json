{
  "name": "parse-server-example",
  "description": "app.json file",
  "scripts": {
  },
  "env": {
    "APP_ID": {
      "required": true
    },
    "MASTER_KEY": {
      "required": true
    },
    "PARSE_MOUNT": {
      "required": true
    }
  },
  "addons": [
    "mongolab"
  ],
  "buildpacks": [
    {
      "url": "urn:buildpack:heroku/nodejs"
    }
  ]
}
